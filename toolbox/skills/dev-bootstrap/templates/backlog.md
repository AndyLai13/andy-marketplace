# {{PROJECT_NAME}} Backlog — 粗略想法暫存區

> lifecycle 第一層。這裡只放**尚未 brainstorm 的想法**：一句話講清楚「要做什麼、為什麼、何時重啟」，**刻意不寫設計細節**——細節會隨產品演進腐爛，真要動工時跑 `superpowers:brainstorming` 拿當下現況重新收斂，產出 `docs/superpowers/specs/<date>-<topic>-design.md`。
>
> 流轉：`backlog.md`（想法）→ `specs/`（brainstorm 設計稿）→ `plans/`（實作計畫）→ `docs/product/`（canonical docs，ship 後吸收）。
> 一個想法被撈進 brainstorm 後，從本檔刪除該段。

---

_尚無 backlog 想法。_
